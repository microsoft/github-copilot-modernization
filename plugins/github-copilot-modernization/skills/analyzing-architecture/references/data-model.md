Analyze entity/model classes, DB config, and ORM mappings. Focus on:
- All entity classes with field inventory
- Relationships (one-to-many, many-to-many, etc.)
- Database schema hints (table names, column mappings)
- Transaction boundaries
- Key entities summary (top 6–8 entities with 1-line description each)

> ⚠️ **Document, do NOT redesign.** This file records the EXISTING data model as-is. Do not suggest schema improvements, renames, or restructuring. The target implementation must preserve this schema exactly.

Output: `./data-model.md`
