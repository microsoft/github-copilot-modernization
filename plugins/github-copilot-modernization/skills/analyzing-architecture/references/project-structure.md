Analyze the codebase at the project root. Focus on:
- Top-level module/package breakdown
- Layer architecture (use whatever layers exist — e.g. controller/service/repository/model, or components/pages/hooks, etc.)
- Entry points (main classes, web descriptors, framework config files, index.html, main.ts, etc.)
- File counts per package/layer
- Key config files and their roles
- List of major functional areas/domains (e.g. Auth, Orders, Catalog, Admin)
- **Project type classification**: backend / frontend / fullstack / library / CLI / monorepo

Output: `./project-structure.md`
