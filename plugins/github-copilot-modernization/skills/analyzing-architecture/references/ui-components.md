Analyze frontend component architecture. Focus on:
- Component tree and hierarchy
- Shared/reusable components vs page-specific ones
- Design system / component library usage (Material UI, Ant Design, etc.)
- Styling approach (CSS modules, Tailwind, styled-components, SCSS, etc.)
- Form handling patterns
- Accessibility patterns

Output: `./ui-components.md`
