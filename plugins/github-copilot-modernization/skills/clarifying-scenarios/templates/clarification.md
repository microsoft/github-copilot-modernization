---
schema: clarification/v1
generated_at: "<UTC ISO-8601 timestamp>"
scope:
  - frontend   # remove if not applicable
  - backend    # remove if not applicable
clarity_score: 0.00   # computed by scoring rubric
rounds: 1             # number of clarification rounds taken (1 = single round, 2 = required follow-up)
gaps: []              # list of fields resolved by default or flagged as blocking
blocking_gaps: []     # required fields with no answer and no default — surfaced as risks
---

<!-- ================================================================
  clarification.md — Canonical clarification artifact (clarification/v1)

  This file is generated by the clarifying-scenarios skill and is
  automatically injected into the dependencyArtifacts of all downstream
  agents (Foundation, Design, Plan, Task, Implementation).

  ⚠️  DO NOT EDIT manually. Re-run the Clarification Gate if inputs change.
  ================================================================ -->

# Scenario Clarification

## Frontend
<!--
  Populated only when scope includes "frontend".
  Format: field label: value  (or "— (default)" if resolution: default)
-->

- **Target framework**: <!-- e.g., React 18 -->
- **Component library**: <!-- e.g., shadcn/ui -->
- **Screenshots**: <!-- e.g., ./screenshots/home.png, ./screenshots/detail.png -->
- **Design system**: <!-- e.g., Figma link or "match existing pixel-by-pixel" -->
- **Accessibility**: <!-- e.g., WCAG 2.1 AA (default) -->
- **Browser targets**: <!-- e.g., modern evergreen (default) -->
- **Responsive strategy**: <!-- e.g., mobile-first, existing breakpoints (default) -->
- **i18n locales**: <!-- e.g., preserve current locales (default) -->
- **State management**: <!-- e.g., Zustand / preserve existing (default) -->
- **Routing**: <!-- e.g., React Router v7 / framework default (default) -->

## Backend
<!--
  Populated only when scope includes "backend".
-->

- **Target framework**: <!-- e.g., Spring Boot 3.2 -->
- **API contract preservation**: <!-- e.g., must preserve -->
- **Data migration strategy**: <!-- e.g., in-place / TBD (default) -->
- **Auth framework**: <!-- e.g., Spring Security 6 / preserve existing (default) -->
- **SLA targets**: <!-- e.g., p95 < 200ms / match current baseline (default) -->

## Generic

- **Success definition**: <!-- e.g., all existing user flows work identically in the new stack -->
- **Out of scope**: <!-- e.g., legacy admin panel, payment gateway integration -->
- **Existing test posture**: <!-- e.g., must pass (default) -->

---

## Gaps & Defaults Applied

<!--
  Auto-populated by the skill.
  Format:
    - id: <field_id>
      resolution: default | user_skipped | blocking
      default_used: "<value>"   (omit for blocking)
      note: "<optional explanation>"
-->

<!-- Example:
gaps:
  - id: i18n.locales
    resolution: default
    default_used: "preserve current locales"
  - id: state_mgmt.preference
    resolution: user_skipped
    default_used: "preserve existing pattern"

blocking_gaps:
  - id: target.component_library
    note: "User skipped a required field. Surfaced as risk in plan summary."
-->

## Downstream Usage Notes

<!--
  Notes for Foundation/Design/Plan agents reading this file.
-->

- Items listed under "Gaps & Defaults Applied" were not explicitly confirmed by the user. Treat them as working assumptions and flag them if they affect critical design decisions.
- Items listed under `blocking_gaps` must be highlighted in the plan summary at the next `[wait]` checkpoint so the user can confirm or provide values before implementation begins.
- Do NOT re-ask the user about items that are fully answered (non-gap) in this file — they count as resolved and do not consume `[NEEDS CLARIFICATION]` budget in the spec phase.
